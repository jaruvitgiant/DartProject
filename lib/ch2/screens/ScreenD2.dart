import 'package:flutter/material.dart';

class ScreenD2 extends StatelessWidget {
  const ScreenD2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Screen D2')),
      body: const Center(child: Text('Screen D2')),
    );
  }
}
